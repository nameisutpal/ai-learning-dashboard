/**
 * Shared spacing scales — import into pages so rhythm stays consistent as the app grows.
 * Using plain strings keeps Tailwind’s JIT happy (no dynamic class names).
 */
export const SECTION_STACK = 'space-y-8 pb-12 md:space-y-10 md:pb-14'

export const PAGE_BOTTOM = 'pb-28 lg:pb-12'

export const COMPACT_SECTION = 'space-y-6 pb-10 md:space-y-8'
